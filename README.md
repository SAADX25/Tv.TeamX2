# 💬 TeamX2 Chat - تطبيق محادثة

تطبيق محادثة شبيه بـ Discord مع دعم الرسائل الفورية ورفع الملفات.

## 🎯 الميزات

- 🔐 **المصادقة**: نظام تسجيل ودخول آمن مع JWT
- 💬 **الرسائل الفورية**: إرسال واستقبال الرسائل في الوقت الفعلي
- 📁 **رفع الملفات**: رفع الصور والملفات (حد أقصى 10 ميجابايت)
- 🎤 **القنوات الصوتية**: دعم أساسي للقنوات الصوتية
- 😀 **الإيموجي**: اختيار وإرسال الإيموجي
- ⌨️ **مؤشر الكتابة**: عرض من يكتب في الوقت الفعلي
- 🌙 **تصميم داكن**: واجهة احترافية بنمط داكن
- 📱 **متجاوب**: يعمل على جميع الأجهزة

## 📁 هيكل المشروع

```
Tv.TeamX2/
├── backend/                  # Backend Node.js
│   ├── server.js            # الخادم الرئيسي
│   ├── models/              # نماذج MongoDB
│   ├── routes/              # مسارات API
│   ├── middleware/          # Middleware للمصادقة
│   └── uploads/             # الملفات المرفوعة
│
├── frontend/                # Frontend
│   ├── index.html          # الصفحة الرئيسية
│   ├── css/                # ملفات CSS
│   ├── js/                 # ملفات JavaScript
│   └── assets/             # الصور والأيقونات
│
└── README.md
```

## 🚀 التثبيت والتشغيل

### 🚀 التشغيل السريع

يمكنك تشغيل المشروع كاملاً بأمر واحد:

```bash
cd backend
npm install
npm start
```

ثم افتح المتصفح على `http://localhost:3000`

### المتطلبات
- Node.js (v16 أو أحدث)
- MongoDB (v5 أو أحدث)

### Backend

1. انتقل إلى مجلد Backend:
```bash
cd backend
```

2. تثبيت التبعيات:
```bash
npm install
```

3. إنشاء ملف `.env`:
```bash
cp .env.example .env
```

4. تعديل `.env` بالإعدادات المناسبة:
```
PORT=3000
MONGODB_URI=mongodb://localhost:27017/teamx2-chat
JWT_SECRET=your-secret-key-here
NODE_ENV=development
```

5. تشغيل السيرفر:
```bash
npm start
# أو للتطوير
npm run dev
```

### Frontend

1. افتح `frontend/index.html` في المتصفح مباشرة
   
   أو استخدم خادم محلي:
```bash
cd frontend
python -m http.server 8080
# أو
npx serve
```

2. افتح المتصفح على:
```
http://localhost:8080
```

## 📖 الاستخدام

1. **إنشاء حساب**: اضغط على "إنشاء حساب" وأدخل بياناتك
2. **تسجيل الدخول**: أدخل البريد الإلكتروني وكلمة المرور
3. **إرسال رسالة**: اكتب رسالتك واضغط Enter
4. **رفع ملف**: اضغط على زر + أو اسحب الملف
5. **انضم لقناة صوتية**: اضغط على قناة صوتية

## 🛠️ التقنيات المستخدمة

### Backend
- **Express.js**: إطار عمل الويب
- **Socket.IO**: اتصال فوري ثنائي الاتجاه
- **MongoDB**: قاعدة البيانات
- **Mongoose**: ODM لـ MongoDB
- **JWT**: المصادقة
- **Bcrypt**: تشفير كلمات المرور
- **Multer**: رفع الملفات

### Frontend
- **HTML5**: الهيكل
- **CSS3**: التصميم مع متغيرات CSS
- **JavaScript ES6+**: المنطق
- **Socket.IO Client**: الاتصال الفوري
- **Font Awesome**: الأيقونات
- **Cairo Font**: الخط العربي

## 📝 API Endpoints

### Authentication
- `POST /api/auth/register` - إنشاء حساب جديد
- `POST /api/auth/login` - تسجيل الدخول

### Messages
- `GET /api/messages/channel/:channelId` - جلب رسائل قناة
- `POST /api/messages` - إرسال رسالة
- `DELETE /api/messages/:id` - حذف رسالة

### Channels
- `GET /api/channels/server/:serverId` - جلب قنوات سيرفر
- `POST /api/channels` - إنشاء قناة

### Upload
- `POST /api/upload` - رفع ملف

## 🔌 Socket.IO Events

### Client → Server
- `user-join` - الانضمام
- `join-channel` - الانضمام لقناة
- `send-message` - إرسال رسالة
- `typing` - إشارة كتابة
- `join-voice` - الانضمام لقناة صوتية

### Server → Client
- `connect` - الاتصال
- `disconnect` - قطع الاتصال
- `new-message` - رسالة جديدة
- `user-typing` - مستخدم يكتب
- `user-status` - تغيير حالة مستخدم
- `user-joined-voice` - انضمام لقناة صوتية

## 🎨 التخصيص

يمكنك تخصيص الألوان في `frontend/css/style.css`:

```css
:root {
  --bg-primary: #0a0a0f;
  --bg-secondary: #0f0f17;
  --brand-primary: #5865f2;
  --success: #23a55a;
  --danger: #da373c;
}
```

## 🔒 الأمان

- ✅ تشفير كلمات المرور باستخدام bcrypt
- ✅ JWT للمصادقة
- ✅ التحقق من حجم الملفات
- ✅ التحقق من نوع الملفات
- ✅ حماية من XSS
- ✅ CORS محدود

## 🤝 المساهمة

نرحب بالمساهمات! يرجى:

1. Fork المشروع
2. إنشاء فرع للميزة (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push للفرع (`git push origin feature/amazing-feature`)
5. فتح Pull Request

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

## 👥 الفريق

تم تطويره بواسطة فريق TeamX2

## 📞 التواصل

- GitHub: [@SAADX25](https://github.com/SAADX25)
- Repository: [Tv.TeamX2](https://github.com/SAADX25/Tv.TeamX2)

---

**ملاحظة**: هذا مشروع تعليمي. للاستخدام في الإنتاج، يرجى تحسين الأمان وإضافة المزيد من الميزات.
